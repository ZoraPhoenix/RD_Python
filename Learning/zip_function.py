# Zip Function

numbers = [1, 2, 3, 4, 5] # List of numbers
names = ['Alice', 'Bob', 'Charlie', 'David', 'Eve'] # List of names

# Using zip to combine two lists into a list of tuples
results = list(zip(numbers, names,))
print(results)
